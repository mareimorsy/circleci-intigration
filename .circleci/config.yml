version: 2.1

jobs:

  build:
    docker:
      - image: node:19-alpine3.15
    steps:
      - checkout # git clone <my repo>
      - run:
          name: "Build the service"
          command: |
            cd calculator
            pwd
            ls
            npm install
            npm run build



  test:
    docker:
      - image: node:19-alpine3.15
    steps:
      - checkout
      - run:
          name: "Test the service"
          command: |
            cd calculator
            npm install
            npm run build
            npm run test


workflows:
  welcome:
    jobs:
      - build
      - test:
          requires:
            - build